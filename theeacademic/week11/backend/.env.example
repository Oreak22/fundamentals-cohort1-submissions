PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL - SQL Choice)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=payverse_db
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration (JWT Choice over Sessions)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# API Configuration (REST Choice)
API_VERSION=v1
CORS_ORIGIN=http://localhost:5173